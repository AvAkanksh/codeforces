#include <bits/stdc++.h>
using namespace std;

#ifdef ONLINE_JUDGE
#define debug(x)
#else
#include "akanksh.h"
#endif

typedef long long ll;

void solve(){
  
}

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    ll t;
    cin >> t;
    while (t--){
        solve();
    }
    return 0;
}